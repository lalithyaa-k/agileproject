pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo "Code checkout done automatically by Jenkins"
            }
        }

        stage('Build') {
            steps {
                echo "Building project..."
            }
        }

        stage('Test') {
            steps {
                echo "Running tests..."
            }
        }
    }
}
